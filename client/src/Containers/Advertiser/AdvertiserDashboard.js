import { connect } from 'react-redux';
import AdvertiserDashboard from '../../Components/Dashboard/AdvertiserDashboard/AdvertiserDashboard';

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = (dispatch) => ({

})

export default connect(mapStateToProps, mapDispatchToProps)(AdvertiserDashboard)